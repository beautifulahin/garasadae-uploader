// 이 판이 **개인판**인가 — 공개 릴리스에 없는 것(쪽지)이 들어 있다.
//
// ★왜 따로 파일인가 — 처음엔 이 값을 `paths.ts` 의 APP_VERSION 바로 아래에 뒀다.
//   그랬더니 공개판이 버전을 올릴 때마다 **같은 줄 언저리가 부딪혀** 자동 합치기가
//   매번 실패했다(2026-08-20 첫 릴리스에서 바로 겪었다). 개인판만 가진 것은
//   공개판이 건드리지 않는 **별도 파일**에 둬야 조용히 합쳐진다.
//
// ★개인판은 스스로 업데이트하지 않는다. 공개판을 받아 덮으면 쪽지 기능이 조용히
//   사라지고, 그 뒤로는 설명 없이 올라가는데 알아채기 어렵다. 갈아끼우기는
//   `개인판.sh` 가 한다 — 공개판을 받아 그 위에 쪽지를 얹어 다시 짓는다.
export const PRIVATE_BUILD = true;
